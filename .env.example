# URL da API local do backend (planejador-backend). Usada só a partir do Marco 4 —
# nos Marcos 1–3 o frontend roda 100% sobre localStorage (chave planejador:v2).
# Sem header de auth: o backend é local, aberto e single-user.
VITE_API_URL=http://localhost:8000/api/v1
